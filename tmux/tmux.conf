# Pass extended keys (CSI-u / kitty keyboard protocol) through tmux
# so Shift+Enter and other modified keys reach the inner application
# (e.g. Claude Code, which uses Shift+Enter for newline).
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
